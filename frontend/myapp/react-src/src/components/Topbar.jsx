import React from 'react';

const Topbar = () => {
  return (
    <header className="topbar">
      <nav>
        <button>Favortios</button>
        <button>Sala de Estar</button>
        <button>Cocina</button>
        <button>Dormitorio</button>
        <button>Ver Más</button>
      </nav>
    </header>
  );
}
export default Topbar;